package elements;

import org.openqa.selenium.By;

public class Button extends BaseElement {
    public Button(By locator) {
        super(locator); }
    // можно добавить клик с JS или удержание, если надо
}
